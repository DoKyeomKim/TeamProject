<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prj.review.mapper.ReviewMapper">

  <select id="getReviewList">
  
    SELECT  COMU_ID,
			NAME,
			TITLE,
			CONTENT,
			STAR
	  FROM  REVIEW_VIEW
  
  </select>

</mapper>